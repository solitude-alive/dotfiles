name: test-macos

on:
  pull_request:
  push:
    branches: [main, dev]

jobs:
  bats:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats
        run: brew install bats-core

      - name: Run integration tests
        run: bats tests/
